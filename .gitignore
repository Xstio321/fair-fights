# ──────────────────────────────────────────────
# Alles blocken, dann gezielt das freigeben,
# was die Webseite tatsächlich braucht.
# ──────────────────────────────────────────────
*

# .gitignore selbst muss erlaubt sein, sonst greift sie auf sich selbst nicht
!.gitignore

# ──────────────────────────────────────────────
# Web-Frontend: explizit erlauben
# ──────────────────────────────────────────────
!index.html
!ranking.html
!8v8.html

!data.json
!data_8v8.json
!ranking.json
!ranking_8v8.json
!fight_history.json
!fight_history_8v8.json
!profiles.json
!podium_history.json

!icons/
!icons/**

# Falls CSS/JS als eigene Dateien existieren (Namen ggf. anpassen)
!*.css
!*.js!home.html
